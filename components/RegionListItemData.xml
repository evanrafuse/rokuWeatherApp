<?xml version="1.0" encoding="utf-8" ?>
 
<component name="RegionListItemData" extends="ContentNode">
  <interface> 
    <!-- labelText goes on the actual Weather Report -->
    <!-- cityName is used for the API because it doesn't include the province -->
    <field id="labelText" type="string" />
    <field id="posterUrl" type="string" />
    <field id="cityName" type="string" />
  </interface>
</component>
